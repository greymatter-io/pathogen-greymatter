{
    "zone_key": "{{ variable "zone" }}",
    "listener_key": "{{ variable "serviceName" }}.egress.listener",
    "domain_keys": [
        "{{ variable "serviceName" }}.egress.domain"
    ],
    "name": "egress",
    "ip": "localhost",
    "port": {{ variable "sidecarEgressPort" }},
    "protocol": "http_auto",
    "tracing_config": null
}
